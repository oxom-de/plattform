/**
 * Brand configuration — driven by environment variables.
 * Set NEXT_PUBLIC_BRAND_NAME in your .env.local to customise.
 */
export const BRAND_NAME = process.env.NEXT_PUBLIC_BRAND_NAME || "plattform";
